{
  "name": "notion-claude-bridge",
  "version": "1.0.0",
  "description": "Local poller that watches a Notion board and triggers Claude Code agents for ticket review and execution",
  "type": "module",
  "scripts": {
    "start": "tsx index.ts",
    "dry-run": "tsx index.ts --dry-run",
    "once": "tsx index.ts --once"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.0",
    "@notionhq/client": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
